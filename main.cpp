#include <iostream>
#include "Vector3.h"
#include "PhysicBody.h"

using namespace std;

int main() {
    /*PhysicBody b(10);
    double dt = 0.01;
    cout << b.getPosition().toString() << endl;
    for (int i = 0; i < 100; ++i) {
        b.step(dt);
        cout << b.getPosition().toString() << endl;
    }*/
    return 0;
}
